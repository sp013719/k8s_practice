apiVersion: v1
kind: Pod
metadata:
  name: kubernetes-demo-pod
  labels:
    app: demoApp
spec:
  containers:
    - name: kubernetes-demo-container
      image: sp013719/rest-api:v1.0.5
      ports:
        - containerPort: 8080
